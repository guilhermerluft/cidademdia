name: CI

on:
  push:
    branches:
      - main
      - 'feat/**'
      - 'fix/**'
      - 'chore/**'
  pull_request:
    branches:
      - main

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  backend:
    name: Backend (.NET 10)
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET 10
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      - name: Restore
        run: dotnet restore CidadeEmDia.sln

      - name: Build
        run: dotnet build CidadeEmDia.sln -c Release --no-restore

      - name: Test
        run: dotnet test CidadeEmDia.sln -c Release --no-build --logger 'trx;LogFileName=test-results.trx'

  frontend:
    name: Frontend (Node 22)
    runs-on: ubuntu-latest
    timeout-minutes: 10
    defaults:
      run:
        working-directory: apps/web

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node 22
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install dependencies
        run: npm install --no-audit --no-fund

      - name: Architecture guard
        run: bash ../../infra/scripts/frontend-architecture-test.sh

      - name: Admin management console guard
        run: bash ../../infra/scripts/admin-management-console-guard.sh

      - name: Occurrence media presentation guard
        run: bash ../../infra/scripts/occurrence-media-presentation-guard.sh

      - name: Public occurrence details guard
        run: bash ../../infra/scripts/public-occurrence-details-guard.sh

      - name: Public home commercial guard
        run: bash ../../infra/scripts/public-home-commercial-guard.sh

      - name: Public plans commercial guard
        run: bash ../../infra/scripts/public-plans-commercial-guard.sh

      - name: Typecheck
        run: npm run typecheck

      - name: Build
        run: npm run build
